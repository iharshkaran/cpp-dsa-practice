#include<iostream>
using namespace std;
int main(){
    int a;//Not condition Its Expression But Not 0(it mean False)
    if(3+2%5) cout<<"this works"<<endl;//Boolean
    if(a=10) cout<<"Even this works"<<endl;
    if(-5) cout<<"Surprisingly Even this works"<<endl;
}